{{#> AuthLayout title=title description=description logo=logo }}
  {{#each inputs as |input|}}
    {{{ InputComponent input=input}}}
  {{/each}}
  {{{ ButtonComponent size=button.size style=button.style type=button.type label=button.label width=button.width}}}
  <p>Signed up already? {{{LinkComponent link=link }}}</p>
{{/AuthLayout}}
