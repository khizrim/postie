{{#> PanelWrapper border='top'}}
  <div class="chat-input">
    {{{ ButtonComponent icon=attachButton.icon label=attachButton.label type=attachButton.type width=attachButton.width isIconOnly=attachButton.isIconOnly onClick=attachButton.onClick }}}
    {{{ InputComponent ref=input.ref name=input.name placeholder=input.placeholder type=input.type onChange=input.onChange required=input.required }}}
    {{{ ButtonComponent icon=sendButton.icon label=sendButton.label type=sendButton.type width=sendButton.width isIconOnly=sendButton.isIconOnly onClick=sendButton.onClick }}}
  </div>
  {{/PanelWrapper}}
