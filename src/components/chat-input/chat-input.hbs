{{#> PanelWrapper border='top'}}
  <div class="chat-input">
    {{{ ButtonComponent icon=attachButton.icon label=attachButton.label type=attachButton.type width=attachButton.width isIconOnly=attachButton.isIconOnly onClick=attachButton.onClick }}}
    {{{ InputComponent name=input.name placeholder=input.placeholder type=input.type required=input.required }}}
    {{{ ButtonComponent icon=sendButton.icon label=sendButton.label type=sendButton.type width=sendButton.width isIconOnly=sendButton.isIconOnly }}}
  </div>
  {{/PanelWrapper}}
